using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlacesOfInterest : MonoBehaviour
{
    private GameObject[] PlaceOfInterest;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //if (Input.GetKeyDown(KeyCode.JoystickButton9)) 
        //{
        //    foreach (GameObject placeOfInterest in PlaceOfInterest)
        //    {
        //        for (int i = 0; i < length; i++)
        //        {
        //            PlaceOfInterest[i].SetActive(true);
        //        }
        //    }
        //}
    }
}
